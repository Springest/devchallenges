%h3= flash[:alert]
%h3= flash[:error]
- if @survey.avaliable_for_participant?(@participant)
  = render 'form'
- else
  %p
    = @participant.name
    spent all the possible attempts to answer this Survey
